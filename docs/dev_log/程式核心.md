# 程式核心 開發紀錄

### 2026-05-18 — V1 核心邏輯實作

**環節：** 程式
**AI 工具：** Kiro Agent
**做了什麼：** 實作 V1 射龍門 Slot 核心邏輯四支 C# 腳本：SlotEngine（盤面生成）、DragonGateJudge（穿門判定）、PayoutCalculator（派彩計算）、GameManager（遊戲流程控制）
**Before：** unity/ 目錄僅有 README.md，無任何程式碼
**After：** 產出 4 支核心腳本於 unity/Assets/Scripts/Core/：
- SlotEngine.cs — 3×3 盤面隨機生成（A~K + 5% Scatter）
- DragonGateJudge.cs — 每列獨立判定（正常門：穿門/碰壁/未穿；同值門：扣款/未穿）
- PayoutCalculator.cs — 賠率對照（極窄6x/窄4x/中2x/寬1x/碰壁1.2x/同值-3x）
- GameManager.cs — 完整一輪流程（下注→Spin→判定→派彩→更新餘額）

**關鍵 prompt / 指令：** 開始 V1 Unity 專案的核心邏輯開發，完成 Spin→判定→派彩 主循環
**人工修正：** 無
**耗時：** ~5 分鐘

### 2026-05-18 — Review BUG 修正（餘額保護 / Gap 語義 / 極窄門定義）

**環節：** 程式
**AI 工具：** Kiro Agent
**做了什麼：** 修正 Review 發現的 3 個 BUG
**Before：** 餘額檢查僅 ≥ 1×bet；GateWidth 用 hi-lo-1 語義混亂；極窄門閾值錯誤
**After：**
- BUG-1: GameManager 餘額保護改為 `_balance < _bet * 10`（保護全盤最壞情況）
- BUG-2: DragonGateJudge 將 GateWidth 改名 Gap，值 = hi - lo（碰壁不設 Gap）
- BUG-3: PayoutCalculator 閾值修正：gap=2→6x / 3-4→4x / 5-8→2x / 9-12→1x

**關鍵 prompt / 指令：** 根據 Review 結果修正餘額保護、Gap 語義統一、極窄門定義
**人工修正：** 無
**耗時：** ~3 分鐘

### 2026-05-19 — Web 版核心移植（C# → JavaScript）

**環節：** 程式
**AI 工具：** Kiro Agent
**做了什麼：** 將 Unity C# 核心邏輯完整翻譯為純 JavaScript Web 版，可直接在瀏覽器運行
**Before：** 僅有 Unity C# 版本，無法快速展示
**After：** 產出 web/ 目錄完整可運行遊戲：
- web/js/slotEngine.js — 3×3 盤面生成（A~K + 5% Scatter）
- web/js/dragonGateJudge.js — 穿門判定（穿門/碰壁/未穿/同值命中）
- web/js/payoutCalculator.js — 賠率計算（gap=2→6×, 3-4→4×, 5-8→2×, 9-12→1×）
- web/js/gameManager.js — 遊戲流程控制（餘額保護 10×押注）
- web/js/uiController.js — UI 控制（盤面/按鈕/結果顯示）
- web/css/style.css — 國潮風深色主題
- web/index.html — 主頁面，打開即玩

**關鍵 prompt / 指令：** 將現有 Unity C# 核心邏輯翻譯成純 JavaScript Web 版
**人工修正：** 無
**耗時：** ~5 分鐘

### 2026-05-19 — Web 版對齊技術規格書（全面修正）

**環節：** 程式
**AI 工具：** Kiro Agent
**做了什麼：** 按技術規格書全面修正 web/ 遊戲邏輯
**Before：** 邏輯沿用舊版 C# 翻譯，Scatter 會跳過判定、gap 語義錯誤、payout 計算偏高
**After：** 5 支 JS 全部重寫對齊規格書：
- slotEngine.js — cell={value, isScatter}，兩者獨立產生
- dragonGateJudge.js — 移除 Scatter 跳過、gap=hi-lo-1、回傳 scatterCount
- payoutCalculator.js — 每列乘數作用於 bet/3（sumMult/3×bet）
- gameManager.js — 餘額保護改 balance≥3×bet
- uiController.js — 顯示牌值+🐉標記、scatter 計數

**關鍵 prompt / 指令：** 按照技術規格書全面修正 web/ 的遊戲邏輯
**人工修正：** 無
**耗時：** ~5 分鐘

### 2026-05-19 — Web 版對齊正確版本（碰壁賠雙、先扣3×bet）

**環節：** 程式
**AI 工具：** Kiro Agent
**做了什麼：** 全面修正 web/ 對齊正確版本邏輯
**Before：** 碰壁為贏 1.2×、payout 計算方式不正確、SC 不跳過判定
**After：** 5 支 JS 全部重寫：
- slotEngine.js — SC 格 value=0，與牌值互斥
- dragonGateJudge.js — SC 跳過判定、碰壁 mult=-2（賠雙）、同值命中 mult=-3
- payoutCalculator.js — 穿門 +betPerRow×mult、碰壁 -betPerRow×2、同值 -betPerRow×3
- gameManager.js — 先扣 3×bet，結算 balance+=totalPayout
- uiController.js — 碰壁/同值顯示為扣款

**關鍵 prompt / 指令：** 按照之前正確版本的邏輯全面修正 web/
**人工修正：** 無
**耗時：** ~3 分鐘
